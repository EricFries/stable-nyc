<% if in_database? %>

	<h3><%= "#{@building.building_num} #{@building.street} #{@building.street_suffix}, #{@building.city}, NY #{@building.zip}" %></h3>
	<h4>At least one apartment in this building is rent-stabilized!</h4>
	<p class="result">To learn more about a specific unit in this building, including its rent history, call the <a href="http://www.nyshcr.org/AboutUs/contact.htm" target="_blank">Division of Homes and Community Renewal's</a> Infoline at 718-739-6400 or visit the following office.</p>

<% else %>

	<h3><%= "#{@building.building_num} #{@building.street} #{@building.street_suffix.downcase.capitalize}" %></h3>
	<h4>This building isn't in our records. Check with DHCR to see if the units here are rent stabilized.</h4>

<% end %>